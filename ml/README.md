# TruthLens ML Model

The trained model files are kept outside this prepared package because the
uploaded ML archive is password-protected.

Expected files:
- `lr_model.jb`
- `vectorizer.jb`

Do not commit the training environment (`venv/`) or other generated files.
If the original ZIP password is available, these files can be added here
after verifying them.
